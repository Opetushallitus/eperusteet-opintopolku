.perusopetus
  .tekstisisalto(ng-repeat='item in section.items', ng-class='section.naviClasses(item)', ng-hide='!item.$matched || item.root || (!search.term && (item.$hidden || item.$impHidden)) || item.$piilotettu')
    a(ng-if='item.$vlk', ng-click='getVlk(item.$vlk)', ng-href='{{item.url}}')
      span {{ item.label | kaanna }}
    a(ng-if='!item.$vlk', ng-href='{{item.url}}')
      span {{ item.label | kaanna }}
      i.pull-right(ng-class='section.naviClasses(item)')
