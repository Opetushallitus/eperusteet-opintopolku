.perusopetus
  .tekstisisalto(ng-repeat='item in section.items', ng-class='section.naviClasses(item)', ng-hide='item.root || item.$hidden || (search.term && (item.$hidden || item.$impHidden))')
    a(ng-href='{{item.url}}')
      span(ep-highlight='search.term', ng-bind='item.label | kaanna') {{ item.label | kaanna }}
